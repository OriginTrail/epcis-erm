name: generate plantumls
on: push
jobs:
  generate_plantuml:
    runs-on: ubuntu-latest
    name: plantuml
    steps:
    - name: checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 1
    - name: generate_from_turtle
      id: ttl
      run:
        bash ci/diagrams-to-puml.sh
    - name: Commit changes
      uses: EndBug/add-and-commit@v5
      with:
        author_name: Plamen Tarkalanov
        author_email: ptarkalanov@abv.bg
        message: 'commit autogenerated puml'
        add: '*.puml'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

